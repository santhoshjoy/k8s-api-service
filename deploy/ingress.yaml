apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: user-api-ingress
spec:
  rules:
  - http:
      paths:
      - path: /users
        pathType: Prefix
        backend:
          service:
            name: user-api
            port:
              number: 80